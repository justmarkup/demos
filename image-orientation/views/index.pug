extends layout

block content

    div.wrapper

        h1(class="hl--alpha") Upload

        form(action="./upload", enctype="multipart/form-data", method="POST", class="form")
            label(for="file") Select images: 
            input(type="file", required, name="file", multiple, accept="image/*")
            input(type="submit", value="Upload your photos")

